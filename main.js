$('#singleWord').submit(function(event){
    event.preventDefault();
    var word = $("#inputSingleWord").val()
    console.log(word);
})

// Form Validation, 5 letters, english word (free word library for this I believe)


// Separating Word into an array

// Letter Positioning in the array (Green, Red)

// Create/Or unlock new inputs

// Saving Stats Display

